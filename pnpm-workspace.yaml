packages:
  - "apps/*"
  - "packages/*"

# Allow these dependencies' install scripts to run (pnpm blocks them by default).
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
  - lefthook
  - sharp

allowBuilds:
  better-sqlite3: true
  esbuild: true
  lefthook: true
  sharp: true
