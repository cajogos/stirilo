packages:
  - "apps/*"
  - "packages/*"

# Allow these dependencies' install scripts to run (pnpm blocks them by default).
onlyBuiltDependencies:
  - esbuild
  - lefthook
  - sharp

allowBuilds:
  esbuild: true
  lefthook: true
  sharp: true
